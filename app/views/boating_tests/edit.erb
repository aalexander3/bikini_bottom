<h1>Edit <%= @boating_test.test_name %></h1>

<!-- a user can create a
 new boating test by inputting the
 test name, inputting a student's name,
 and selecting the instructor -->
<%= form_for @boating_test do |f| %>
  <%= f.label :test_name %>
  <%= f.text_field :test_name %>
  <br>
  <%= f.label :completed %>
  <%= f.check_box :completed %>
  <br>
  <%= f.submit %>
<% end %>
